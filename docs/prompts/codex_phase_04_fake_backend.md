# Codex Prompt — Phase 4 Fake PHP Backend

Create a fake PHP backend that matches the final API and realtime protocol.

Read:

```text
docs/03_api_contract.md
docs/04_realtime_protocol.md
docs/user-stories/
```

Implement deterministic fake responses and WebSocket updates for dev scenarios.

Do not call real Entur.
Do not require SurrealDB.

The frontend should be able to switch from local fixtures to this backend without changing UI state types.
