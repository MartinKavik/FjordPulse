# Codex Prompt — Phase 3 SolidJS Visual Prototype

Implement deterministic UI prototype states.

Read the design bundle:

```text
docs/design/
```

Create route/state switcher pages for:

```text
default map
station loading/fresh/empty/stale/error
vehicle selected/focus/paused/stale/lost
fallback mode
search results/search empty
mobile states
admin status/watches/Entur log
```

Use local TypeScript fixtures only.

Do not use real backend yet.

Output should be visually close to the design bundle and ready for screenshot testing.
