# FjordPulse Contracts

These drafts are canonical boundaries shared by fake adapters, real adapters, PHP DTOs/validators, TypeScript types/validators, and tests.

They are intentionally incomplete enough to evolve during implementation but concrete enough to prevent frontend/backend drift.
